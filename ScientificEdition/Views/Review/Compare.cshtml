@* @model Tuple<ArticleVersion, ArticleVersion>

@{
    ViewData["Title"] = "Порівняння версій статті";
}

<div class="container mt-5">
    <h2 class="text-center">Порівняння версій статті</h2>

    <div class="row border p-4 mb-3">
        <div class="col-md-6 border-end">
            <h4 class="text-primary">Версія 1</h4>
            <h5>@Model.Item1.Title</h5>
            <p><strong>Дата завантаження:</strong> @Model.Item1.UploadDate.ToString("g")</p>
            <div class="content-box">
                @Html.Raw(Model.Item1.Content)
            </div>
        </div>

        <div class="col-md-6">
            <h4 class="text-primary">Версія 2</h4>
            <h5>@Model.Item2.Title</h5>
            <p><strong>Дата завантаження:</strong> @Model.Item2.UploadDate.ToString("g")</p>
            <div class="content-box">
                @Html.Raw(Model.Item2.Content)
            </div>
        </div>
    </div>

    <div class="mt-4 text-center">
        <a asp-action="Index" class="btn btn-secondary">Назад до списку статей</a>
    </div>
</div>

<style>
    .content-box {
        background-color: #f8f9fa;
        padding: 15px;
        border-radius: 5px;
        min-height: 200px;
    }

    .text-primary {
        color: #007bff;
    }

    .border-end {
        border-right: 1px solid #ddd;
    }

    .btn {
        min-width: 150px;
        margin-top: 15px;
    }
</style>
 *@