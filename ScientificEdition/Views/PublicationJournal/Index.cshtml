@* @model IEnumerable<PublicationJournal>

@{
    ViewData["Title"] = "Список публікацій";
}

<div class="container mt-5">
    <h2 class="text-center">Список публікацій</h2>

    <table class="table table-bordered table-hover">
        <thead>
            <tr>
                <th>Заголовок</th>
                <th>Дата публікації</th>
                <th>Автор</th>
                <th class="text-center">Дії</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var publication in Model)
            {
                <tr>
                    <td>@publication.Title</td>
                    <td>@publication.PublicationDate.ToString("dd.MM.yyyy HH:mm")</td>
                    <td>@(publication.Author ?? "Невідомий автор")</td> <!-- Додано перевірку на null -->
                    <td class="text-center">
                        <a asp-action="Edit" asp-route-id="@publication.Id" class="btn btn-warning btn-sm">Редагувати</a>
                        <a asp-action="Delete" asp-route-id="@publication.Id" class="btn btn-danger btn-sm">Видалити</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    <div class="mt-4 text-center">
        <a asp-action="Create" class="btn btn-success">Додати нову публікацію</a>
    </div>
</div>

<style>
    h2 {
        font-weight: bold;
        color: #343a40;
    }

    .table-bordered th {
        background-color: #f8f9fa;
        font-weight: bold;
        text-align: center;
    }

    .table-bordered td, .table-bordered th {
        vertical-align: middle;
    }

    .btn {
        margin-right: 5px;
        transition: all 0.2s;
    }

        .btn:hover {
            transform: scale(1.05);
        }

    .text-center a.btn-success {
        margin-top: 15px;
        padding: 10px 20px;
    }
</style>
 *@